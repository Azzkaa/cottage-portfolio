# README media

Drop the portfolio recordings here. The root `README.md` references these
exact filenames:

| File | Content |
|---|---|
| `hero.gif` | START → camera sweep → slow orbit of the shop |
| `plaques.gif` | Hovering then clicking a couple of signpost plaques |
| `minigames.gif` | A few seconds of each of the four mini-games |

Record the live scene at `localhost:5173`. Keep clips short (5–10s,
~820px wide) so the page stays light. Until the files are added, the
images in the main README will show as broken — that's expected.
